# build & deps
node_modules/
.next/
out/
coverage/
dist/

# env & local
.env*
!.env.example
!env.example
*.local

# logs
logs/
var/
web.log

# editor cruft
.history/
.DS_Store
Thumbs.db

# ops artifacts
ops/artifacts/*
*.zip
*.tsbuildinfo
.cookies.backend.txt

# temp/test artifacts
*.cookies.txt
*.cookie.txt
*.csrf.txt
csrf-token.txt
*-test.http
*-verification.http
*-bff.http
*.pid
test-login.json
login.json
debug-session.sql

# PostgreSQL backups
*.sql.backup
vah_backup.sql

# request sandboxes & cookies/csrf
*.http

# misc local junk folders
_archive/
-Force/
backups/

# dev-only tooling we don't deploy
tools/mock-api/

# test outputs (keep the directory; ignore artifacts)
test-results/**
!test-results/.gitkeep

# === Block frontend artifacts in backend repo ===
.next/
out/
app/
pages/
components/
public/
styles/
assets/
src/app/
src/pages/
src/components/
next.config.*
postcss.config.*
tailwind.config.*
vite.config.*
webpack.config.*
.babelrc*
components.json
vercel.json
playwright.config.*
jest.config.js

# === Allow backend TypeScript source ===
!src/server/
!src/server/**
