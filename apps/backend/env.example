# Database Configuration
DATABASE_URL=postgresql://libanadan@localhost:5432/test
DB_CLIENT=pg
DISABLE_SQLITE=true

# Development Settings
NODE_ENV=development
PORT=8080

# CORS & Security
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
JWT_SECRET=changeme-in-local
JWT_COOKIE=vah_session
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:8080

# Production Settings (examples - do not commit real values)
APP_ORIGIN=https://virtualaddresshub.co.uk
BACKEND_API_ORIGIN=https://vah-api-staging.onrender.com/api
POSTMARK_TOKEN=change-me
POSTMARK_FROM=no-reply@virtualaddresshub.co.uk

# --- App / URLs
APP_BASE_URL=https://virtualaddresshub.co.uk

# --- Postmark
POSTMARK_TOKEN=your_postmark_server_token
POSTMARK_FROM=hello@virtualaddresshub.co.uk
POSTMARK_FROM_NAME=VirtualAddressHub
POSTMARK_REPLY_TO=support@virtualaddresshub.co.uk
POSTMARK_STREAM=outbound
# Optional: Redirect all non-prod emails to a safe address (e.g., dev-inbox@yourdomain.co.uk)
DEV_EMAIL_OVERRIDE=

# --- Webhook secret (optional but recommended)
# Leave unset for no-auth (CI/staging)
POSTMARK_WEBHOOK_SECRET=your_webhook_secret

# --- Dev routes (staging/local only)
# Generate with: openssl rand -hex 32
DEV_SEED_SECRET=

# --- Rate limiting (optional staging toggle)
DISABLE_RATE_LIMIT=0

# --- Feature flags (1=on)
EMAIL_ONBOARDING=1
EMAIL_BILLING=1
EMAIL_KYC=1
EMAIL_MAIL=1
EMAIL_SUPPORT=1
EMAIL_SECURITY=1

# --- Password reset configuration
PASSWORD_RESET_TOKEN_TTL_MINUTES=30
PASSWORD_RESET_RATE_LIMIT_WINDOW_MINUTES=15
PASSWORD_RESET_RATE_LIMIT_MAX=5
BCRYPT_ROUNDS=12
INVOICE_LINK_TTL_USER_MIN=30
INVOICE_LINK_TTL_ADMIN_MIN=60
VA_LINE1=Your Company Name, 123 Example Street
VA_LINE2=London
VA_POSTCODE=EC1A 1AA

## GoCardless
GOCARDLESS_ACCESS_TOKEN=
GOCARDLESS_WEBHOOK_SECRET=
GOCARDLESS_ENV=sandbox

## Sumsub
SUMSUB_API_KEY=
SUMSUB_WEBHOOK_SECRET=
SUMSUB_BASE_URL=https://api.sumsub.com

## Microsoft Graph (for SharePoint file access)
GRAPH_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
GRAPH_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
GRAPH_CLIENT_SECRET=your_secret_here

# Your SharePoint site for ops@…
GRAPH_SITE_HOST=virtualaddresshubcouk-my.sharepoint.com
GRAPH_SITE_PATH=/personal/ops_virtualaddresshub_co_uk

# Used for internal background job authentication
INTERNAL_CRON_TOKEN=replace-with-a-long-random-string
