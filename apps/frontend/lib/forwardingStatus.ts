// apps/frontend/lib/forwardingStatus.ts
import { FWD_LABEL } from '@vah/shared';
export { FWD_LABEL }; // use labels for badges

export const toApiStatus = (s: string) => s; // now coming from keys already
