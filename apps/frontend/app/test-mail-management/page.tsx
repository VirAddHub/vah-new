// This page was a development-only test fixture with hardcoded mock mail data.
// It is not part of any navigation or user journey.
// Always returns 404 — delete this file once the build has been verified clean.
import { notFound } from 'next/navigation';

export default function TestMailManagement() {
    notFound();
}
