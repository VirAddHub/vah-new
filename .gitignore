# build & deps
node_modules/
.next/
out/
coverage/
dist/

# env & local
.env*
!.env.example
!.env.production.example
!env.example
*.local

# data & logs
data/*.db*
data/*.db-wal
data/*.db-shm
logs/
var/

# editor cruft
.history/
.DS_Store
Thumbs.db

# ops artifacts
ops/artifacts/*
*.zip
# local db & build
*.db
*.db-*
vah.db*
data/*.db*
*.tsbuildinfo
.cookies.backend.txt

# local db & build
*.db
*.db-*
vah.db*
data/*.db*
*.tsbuildinfo
.cookies.backend.txt


# temp/test artifacts
*.cookies.txt
*.cookie.txt
*.csrf.txt
csrf-token.txt
*-test.http
*-verification.http
*-bff.http
*.pid
test-login.json
login.json
debug-session.sql
web.log

# local SQLite (optional; keep if you use sqlite in dev)
app.db
app.db-shm
app.db-wal
vah_corrupted.db
vah.db.backup

# --- repo hygiene (keep tests, ignore junk) ---

# local db & backups (we use Postgres in prod)
app.db
app.db-shm
app.db-wal
vah_corrupted.db
vah.db.backup
vah_backup.sql

# logs & pids
logs/
web.log
*.pid

# request sandboxes & cookies/csrf
*.http
*.cookies.txt
*.cookie.txt
*.csrf.txt
csrf-token.txt

# misc local junk folders
.history/
_archive/
-Force/
backups/

# dev-only tooling we don't deploy
tools/mock-api/

# test outputs (keep the directory; ignore artifacts)
test-results/**
!test-results/.gitkeep


# === Block frontend artifacts in backend repo ===
.next/
out/
app/
pages/
components/
public/
styles/
assets/
src/app/
src/pages/
src/components/
next.config.*
postcss.config.*
tailwind.config.*
vite.config.*
webpack.config.*
.babelrc*
components.json
vercel.json
playwright.config.*
jest.config.js
