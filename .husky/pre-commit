#!/usr/bin/env bash
# .husky/pre-commit
# Pre-commit hook with safety checks

echo "🔒 Running pre-commit safety checks..."

# Run the lowercase status check
bash scripts/prevent-lowercase-status.sh || exit 1

# Run existing pre-commit checks if they exist
if [ -f "scripts/pre-commit-checks.sh" ]; then
  bash scripts/pre-commit-checks.sh || exit 1
fi

echo "✅ All pre-commit checks passed!"
